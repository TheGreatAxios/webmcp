{
  "name": "@thegreataxios/webmcp-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@thegreataxios/*' build",
    "test": "bun test",
    "typecheck": "bun run --filter '@thegreataxios/*' typecheck",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "tsup": "^8.5.0",
    "@types/bun": "latest"
  }
}
