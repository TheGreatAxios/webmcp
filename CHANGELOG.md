# Changelog

All four packages (`@thegreataxios/webmcp-core`, `-react`, `-bridge`,
`@thegreataxios/webmcp`) are versioned in lockstep.

## [Unreleased]

## [0.1.0] — initial public release

- Spec-aligned `document.modelContext` polyfill, tool registry, validation.
- React provider, tools, and `experimental_*` sync / journeys / confirmations / bridge.
- `webmcp-bridge` CLI: stdio MCP ↔ localhost WebSocket.
- Six themed Vite + React examples.
